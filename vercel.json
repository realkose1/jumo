{
  "buildCommand": "echo 'no build step — static site + serverless functions'",
  "installCommand": "echo 'no dependencies needed for web deploy'",
  "outputDirectory": ".",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/:path*" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
